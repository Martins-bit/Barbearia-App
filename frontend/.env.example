# URL base da API do backend (NestJS).
# Em desenvolvimento, o backend roda por padrão em http://localhost:3000.
# Copie este arquivo para `.env.local` e ajuste se necessário.
VITE_API_URL=http://localhost:3000
